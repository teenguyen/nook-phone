import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import NookPhone from './NookPhone';

ReactDOM.render(
  <React.StrictMode>
    <NookPhone />
  </React.StrictMode>,
  document.getElementById('nookphone')
);
